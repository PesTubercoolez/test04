spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/matrix_db
    username: root
    password: zetaprime1
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
    open-in-view: false


